services:
  postgres:
    image: postgres:15
    container_name: fice-postgres
    restart: always
    environment:
      POSTGRES_DB: fice
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"